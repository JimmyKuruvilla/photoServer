const filesStr = 'files';
const dirsStr = 'dirs';
const mediaStr = 'media';
const port = 4000;

const redirectItem = {
  name: 'isRedirectToRandom',
  webPath: 'random'
};

module.exports = {
  filesStr,
  dirsStr,
  mediaStr,
  redirectItem,
  port
};