const filesStr = 'files';
const dirsStr = 'dirs';
const mediaStr = 'media';
const port = 4000;
const defaultInterval = 3000;

module.exports = {
  filesStr,
  dirsStr,
  mediaStr,
  port,
  defaultInterval
};